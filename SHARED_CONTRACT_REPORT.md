# DemoPilot 内部共享交互协议实战报告

日期：2026-08-26

## 问题来源

原流程由 Builder 同时生成页面和 `interaction_tests`。当 Builder 编造不存在的选择器或把功能放进不可见页面时，Runner 会沿用 Builder 自报的测试定义，产品、构建和评审容易共享同一个错误假设。这不是缺少 Agent，而是 Agent 之间没有机器可验证的交接协议。

## 实现

Agent Team 新增 Contract Agent，位于产品/体验/评审标准之后、Builder 之前。Contract Agent 只描述每项 must-have 的业务页面、操作步骤和成功反馈，不允许编写选择器。Harness Compiler 再为每条需求分配稳定且唯一的 `#contract-*` 选择器，补齐缺失需求，生成 SHA-256，并把协议冻结。

同一份 `interaction_contract` 同时提供给：

- Builder：必须实现协议，不能重命名或自行发明验收路径。
- Runner：忽略 Builder 自报测试，只执行冻结协议。
- Reviewer：根据协议、最终文件和 Chromium 证据判断通过或返工。

协议只生成一次。返工可以改实现，不能修改协议或降低验收标准。

## 确定性验证

Mock 全流程运行 `bdbb17982ceb`：9 次 Agent 调用，协议检查通过、Chromium 通过、最终质量门通过。编译器单元测试额外验证了模型即使返回 `#model-invented-selector`，最终协议也只使用 Harness 分配的稳定选择器。

## 真实 DeepSeek 首轮测试

评测 `a1ef0848f5b4` 复用了此前相同三个高难用例，并强制首轮停止：

| 指标 | 共享协议版本 | 前一获批 Skill 版本 | 变化 |
| --- | ---: | ---: | ---: |
| 首轮成功率 | 33.3% | 33.3% | 持平 |
| 首轮平均分 | 42.3 | 55.3 | -13.0 |
| 浏览器通过率 | 33.3% | 33.3% | 持平 |
| 功能覆盖率 | 66.7% | 100% | -33.3 个百分点 |
| 平均调用数 | 9 | 8 | +1 |

因此不能声称共享协议提高了首轮总体质量。零售用例仍因不安全 HTML 被安全 Hook 阻断；物流用例首轮 100 分通过；采购用例不再出现含糊的共同幻觉，而是精确报告缺少 `#contract-3-step-1`、`#contract-3-step-2` 以及成功反馈文本不匹配。

## Agent Team 自主返工验证

随后对采购用例运行允许返工的真实评测 `4ad9771a9d64`，Demo 运行 ID 为 `3e17704311c6`。首轮协议检查失败后，Reviewer 把缺失选择器和复验路径交给 Builder；Agent Team 自主返工 2 次，最终结果：

- 95 分通过；
- Chromium 真实交互通过；
- 最终质量门通过；
- 协议 SHA-256 全程冻结：`5fa89e21e19288e310d22b79832067e49a0c80d2b7eeddbb3a47c32de8865f96`；
- Contract Agent 只调用 1 次，没有在返工时改写验收标准；
- 第 0 轮协议实现失败，第 1、2 轮协议结构检查通过，最终浏览器行为通过。

## 实战结论

这个改动的直接价值不是让模型第一次必然成功，而是把“团队共同相信同一个幻觉”变成“可定位、可修复、不可偷偷降低标准的接口违约”。它显著提高了失败可诊断性和自主返工的确定性，但当前没有证据证明首轮成功率上升。

下一步应增加 Builder 返回后的低成本协议预检，在写入沙箱和启动 Chromium 前就拒绝缺失选择器的回答；安全 HTML 失败需要单独优化，不应混入交互协议职责。
