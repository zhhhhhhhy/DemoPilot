# DemoPilot Builder 确定性预检报告

日期：2026-08-27

## 结论

已实现 Builder 预检硬门，并确认它会覆盖初版及每一个返修版本。硬门只处理确定性条件；视觉、文案和销售说服力没有被错误地升级为阻断项。

## 实现范围

- 三个静态 Web 文件完整性、额外文件与文件大小预算。
- 密钥、外部网络、远程资源、内联脚本、动态执行和非空 `innerHTML` 注入。
- viewport、本地 CSS/JS、模拟数据边界。
- 冻结契约选择器、原生控件类型和成功反馈文本。
- 基础点击绑定、三幕故事、must-have 数据、客户主色。
- 结构化失败类别、错误码、精确选择器和定向修复说明。
- 每个 Builder 修订版重新预检；失败时不落盘、不启动 Chromium、不调用最终 Reviewer。
- Chromium 失败返回缺失期望值与实际文本摘要。
- Contract v1.1 将描述性长断言拆成短、原子、可见文本。

## 自动化验证

- Ruff：通过。
- 后端定向测试：预检规则、初版安全阻断、Reviewer 返修后重新预检、评测分类均通过。
- 前端 ESLint、Vitest 3 项、生产构建：通过。
- 完整后端测试结果见本次实现终态；Mock 仍明确标记为 `controlled_template_fallback`，不冒充真实模型代码。

## 真实 DeepSeek 失败尝试

### `9a4c3f559ba5`

首版的危险 `innerHTML`、8 个缺失控件和断言缺失在写入前被拦截。后续运行态出现元素隐藏和长断言文本不匹配，4 轮后失败。该次暴露了“返修版未重新预检”和 Chromium 反馈过于模糊的问题。

### `e734b2a88fb6`

逐轮预检生效：迭代 0、1 分别因断言和选择器失败，迭代 2 才进入浏览器；Reviewer 返修后的迭代 3、4 又被预检阻断，没有重复启动浏览器。最终因描述性长断言无法逐字实现而耗尽轮次。该次直接促成 Contract v1.1。

## 真实 DeepSeek 最终复验

任务：`a50e398debcb`

- 场景：复杂零售缺货筛选、排序、门店钻取、方案选择、任务创建与状态推进。
- Provider：DeepSeek，真实外部模型调用。
- Contract：`interaction-contract-v1.1`。
- 迭代 0：缺少 7 个契约控件，预检阻断。
- 迭代 1：缺少 `#contract-2-step-1`，预检阻断。
- 迭代 2：仍缺少 `#contract-2-step-1`，预检阻断。
- 迭代 3：预检通过，随后才进入产物写入、Chromium 和最终 Reviewer 阶段。
- 终态：任务完成；产物验证通过；Chromium 通过；Reviewer 决策 `pass`，100 分；质量门通过。
- 成本：12 次模型调用，3 次返修。

## 边界

本次只证明这一条复杂真实流程可完成，以及门控能减少无效的落盘、浏览器和 Reviewer 工作；不能由单例推断整体首轮成功率提高。下一步仍应使用固定评测集做成对 A/B，比较首轮成功率、最终成功率、浏览器启动次数、Reviewer 调用次数、总调用数和耗时。

## 3 用例 DeepSeek 探索性 A/B

实验时间：2026-08-27

设计：两组均使用 DeepSeek、`approved` Skills、相同 `retail-01`、`logistics-01`、`procurement-01`，允许最多 4 次正常返修。唯一显式变化是评测任务中的 `builder_preflight_enabled`；普通 Demo 不允许通过该开关关闭预检。由于模型温度为 0.1 且没有固定随机种子，这是一轮小样本探索，不是统计显著实验。

### A 组：关闭预检

- 评测 ID：`a66020eb4d98`
- 最终通过：0/3，成功率 0%。
- 平均分：19.0。
- 浏览器通过率：0%。
- 平均模型调用：17.0。
- 平均耗时：285.81 秒。
- 三个用例均执行 5 次最终 Reviewer；浏览器截图写入分别为 5、4、5 次。

### B 组：启用预检

- 评测 ID：`3a687f08ffa7`
- 最终通过：1/3，成功率 33.3%。
- 平均分：32.42。
- 浏览器通过率：33.3%。
- 平均模型调用：12.67。
- 平均耗时：181.50 秒。
- 零售和物流各在 5 次预检失败后停止：各调用 1 次最终 Reviewer、0 次浏览器截图写入。
- 采购最终通过，95.25 分，12 次模型调用、2 次返修。

### 差异与判定

- 最终成功率：+33.33 个百分点。
- 平均分：+13.42。
- 浏览器通过率：+33.33 个百分点。
- 平均模型调用：-4.33。
- 平均耗时：-104.31 秒。
- 当前评测器的功能覆盖率：-66.67 个百分点。

功能覆盖率下降来自两个用例在产物生成前被阻断，评测器将“没有允许生成产物”记为 0 覆盖；A 组虽然最终失败，但曾生成含 must-have 文本的产物，因此静态覆盖仍记为 100%。这是指标语义差异，不能删除或当作无效结果。

结论：门控对减少无效浏览器/Reviewer 工作有直接证据，最终成功率和质量分有初步正向信号；但本轮没有证明首版 Builder 质量提高，而且样本量仅 3、没有固定模型随机种子、功能覆盖指标出现退化。因此保留门控作为确定性安全与成本控制，不把本轮结果宣传为总体质量已被正式证明。下一步需要至少 3 次重复配对，并把“预检阶段契约覆盖”与“最终产物功能覆盖”拆成两个指标。
