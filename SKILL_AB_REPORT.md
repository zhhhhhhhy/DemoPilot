# DemoPilot 工程 Skill A/B 报告

日期：2026-08-26
Provider：DeepSeek
用例：`retail-01`、`logistics-01`、`procurement-01`
控制条件：相同 Provider、相同用例、串行执行、首轮停止、不允许 Reviewer 返工。

## 晋级规则

候选配置需满足以下全部条件：

1. 首轮成功率高于基线；或首轮成功率持平且首轮平均分至少提高 2 分。
2. 浏览器通过率不下降。
3. must-have 功能覆盖率不下降。
4. 平均 Agent 调用数不增加。

## 结果

| 配置 | 评测 ID | 首轮成功率 | 首轮平均分 | 浏览器通过率 | 功能覆盖率 | 平均调用数 | 结论 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| Baseline | `6c528c4e83c1` | 0.0% | 30.0 | 0.0% | 66.7% | 8.0 | 对照组 |
| Candidate v1 | `ddecfeb6f549` | 33.3% | 55.3 | 33.3% | 100.0% | 8.0 | 通过并晋级 |
| Candidate v2 | `e1fde234e27d` | 0.0% | 26.0 | 0.0% | 66.7% | 8.0 | 拒绝并回滚 |

Candidate v1 相对基线：首轮成功率 `+33.3` 个百分点、首轮平均分 `+25.3`、浏览器通过率 `+33.3` 个百分点、功能覆盖率 `+33.3` 个百分点，调用数不变。耗时平均增加约 `0.9s`。

## 失败尝试与处置

Candidate v2 为解决隐藏页面与错误选择器，进一步加强了路由、可见性和选择器约束，但三个用例全部未通过，首轮平均分下降到 26.0。说明继续把所有异常写进提示词会造成过度约束和注意力稀释。该版本未进入 Harness，相关 Skill 已精确回滚至 v1。

v1 仍有两个首轮失败：零售用例出现隐藏区域/筛选器，采购用例出现虚构选择器和隐藏按钮；物流用例首轮 100 分通过。后续应优先把“需求到确定性浏览器路径/选择器”的编译变成结构化工具约束，而不是继续膨胀 Skill 文本。

## 正式接入证据

当前 `approved` 与通过 A/B 的 Candidate v1 为同一配置包，SHA-256：

`ccc27684bd2be1718a0dd26c69757d2c1529f6a195e0211444cb5a276c660d7d`

正式配置已完成额外 Mock 全流程冒烟：评测 `6652c7e8683f`、Demo 运行 `7fe521e430f1`，浏览器验证和质量门均通过，运行轨迹记录了 Skill 包与每个阶段实际加载的 Skill 子集。

## 结论边界

这次实验能证明六个 Skill 组成的 v1 包在三个固定高难用例上显著提高了首轮质量，因此允许进入当前 Harness；它不能证明对所有行业和所有需求都稳定提升。新增或修改 Skill 后必须重新运行 A/B，不能沿用本次晋级结论。
